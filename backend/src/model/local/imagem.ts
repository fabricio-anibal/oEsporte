export class Imagem {
    content: string
    cols: Number
    rows: Number

    constructor(content: string, cols: Number, rows: Number) {
        this.content = content
        this.cols = cols
        this.rows = rows
    }
}